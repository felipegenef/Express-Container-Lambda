version: '3'
services:
  mongo:
    image: mongo
    container_name: mongo
    ports:
      - 27017:27017
    volumes:
      - '/mnt/efs:/data/db'

    # environment:
    #   - MONGO_INITDB_ROOT_USERNAME=value
    #   - MONGO_INITDB_ROOT_PASSWORD=""

